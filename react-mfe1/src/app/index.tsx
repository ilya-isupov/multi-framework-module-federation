import * as React from 'react';
import { hot } from 'react-hot-loader';
import './index.less';
import 'semantic-ui-css/semantic.min.css';

export const App = hot(module)(() => {
    return (
      <div>
        This is react plugin application
      </div>
    );
  })
;
