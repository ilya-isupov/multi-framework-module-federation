import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {Application} from "./application";


ReactDOM.render(
  <Application header="Application root"/>,
  document.getElementById('root')
);
